@if (IsLoading)
{
    <div class="py-1">
        <progress class="progress is-small is-primary" max="100">15%</progress>
    </div>
}

@code {
    [Parameter]
    public bool IsLoading { get; set; }
}